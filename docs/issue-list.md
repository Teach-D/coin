# Issue List — 배틀 매칭 Socket.io 알림 + BattlePage STOMP 잔재 제거

> 생성일: 2026-05-19
> 총 이슈 수: 1개

| 순서 | 번호 | 타입 | 제목 | 선행 이슈 | 브랜치 |
|------|------|------|------|-----------|--------|
| 1 | #42 | fix | 배틀 매칭 완료 Socket.io 알림 + BattlePage STOMP 잔재 제거 | 없음 | `fix/battle-match-socketio-42` |

---

## 브랜치 네이밍 규칙

```
{type}/{kebab-description}-{issue_number}
```

## 작업 시작 방법

```bash
git checkout dev && git pull origin dev
git checkout -b fix/battle-match-socketio-42
```

작업 완료 후:
```
/pr-merge-dev  →  dev 브랜치로 PR 생성 및 머지
```
